logs:
    label: Logs
    readOnly: true
    view:
        recordsPerPage: 10
        recordOnClick: "$.popup({ handler: 'onViewLogDetails', extraData: { record_id: ':id' } })"
        list: $/avalonium/feedback/models/log/columns.yaml
